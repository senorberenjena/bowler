<% form_for @game do |f| %>
    <%= f.label :title, 'Titel' %>:
        <%= f.text_field :title %>

    <div id="players">
        <%= render :partial => 'edit_player', :locals => {:form => f}, :collection => @game.players %>
        <%= render :partial => 'edit_player', :locals => {:form => f} %>
    </div>

    <%= f.submit 'Start Game' %>
<% end %>