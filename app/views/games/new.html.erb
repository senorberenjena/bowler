<% form_for @game do |f| %>
    <p>
        <%= f.label :title, 'Titel' %>: <%= f.text_field :title %>
    </p>
    <div id="players">
        <%= render :partial => 'edit_player', :locals => {:form => f}, :collection => @game.players %>
        <%= render :partial => 'edit_player', :locals => {:form => f}, :object => Player.new %>
    </div>

    <%= f.submit 'Start Game' %>
<% end %>